import { Router } from 'express';
import { course} from '../models/course.model'; // Assuming a Mongoose model for courses

const router = express.Router();

//router.get('/', getAllCourses); 


export default router;
